---
apiVersion: "networking.istio.io/v1alpha3"
kind: "VirtualService"
metadata:
  name: "<clientName-datetime>"
  nameSpace: null
spec:
  hosts:
  - "<hostname>"
  - "newHost.com"
  - "newHost.com"
  gateways:
  - "apiwiz-system/apiwiz-gateway"
  http:
  - route:
    - destination:
      - host: "apibuilder-agent"
        port:
          port: 0
